# Cluster Studio — Static Site

This repository contains a single static HTML file for Cluster Studio.

To preview locally, open `index.html` in your browser or run a simple static server:

```powershell
# from the repo folder
python -m http.server 8000
# or (Node.js)
npx http-server -c-1 .
```
